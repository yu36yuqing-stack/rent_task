# AGENTS.md - rent_task

## 1. 项目定位

`rent_task`（租号小助手）是面向多用户、多游戏、多租号渠道的自动化运营系统，主要负责：

- 同步用户、平台授权、游戏账号和渠道商品状态。
- 同步订单，计算订单规则、冷却期和统计收益。
- 根据黑名单、订单、在线检测和风控规则执行跨渠道上下架。
- 提供 H5 管理端、运行任务、通知告警和生产运维能力。

处理任何任务前必须先阅读 `config/codex_rule.md`。规则冲突时，优先级依次为：用户当前明确要求、当前目录最近的 `AGENTS.md`、根目录 `AGENTS.md`、其他项目文档。

## 2. 事实来源与防幻觉要求

- 业务现状以运行代码、数据库结构和可执行测试为准，PRD/README 只作为导航和设计背景。
- 不得只根据文件名、旧 PRD 或单段代码断言完整行为；必须沿真实入口、调用链、数据读写和测试至少完成一轮交叉验证。
- 明确区分“当前已实现”“根据证据推断”“建议方案”，不能把建议描述成现状。
- 信息不足时先定向读取相关文件或执行只读查询；仍无法确认时列出未知点，不得编造。
- 优先使用搜索和局部读取定位上下文，避免无目标通读全仓库；单次上下文遵循 `config/codex_rule.md` 的最小必要原则。

## 3. 系统核心流程导航

### 3.1 顶层调度主流程

入口：`rent_robot_main.js`

1. 初始化日志、进程锁和数据库迁移检查。
2. 按配置触发订单同步 Worker：`order/order_worker.js`。
3. 按配置触发收益统计 Worker：`stats/order_stats_worker.js`。
4. 异步触发黑名单巡检：`blacklist/blacklist_inspector.js`。
5. 遍历启用用户，为每个用户加数据库锁并执行 `pipeline/user_pipeline.js`。
6. 记录运行任务、执行结果和异常通知，最后释放锁。

### 3.2 用户商品与上下架主链路

入口：`pipeline/user_pipeline.js`

主要链路：

1. `product/product.js` 根据用户的平台授权同步各渠道商品和游戏账号。
2. `order/service/order_rule_service.js` 收敛订单导致的下架和冷却规则。
3. `blacklist/blacklist_source_gateway.js`、`blacklist/blacklist_reconciler.js` 维护多来源黑名单及最终投影。
4. `action_engine/action_engine.js` 比较目标状态和渠道现状，执行各平台上下架动作。
5. 刷新订单、出租中状态等事实，避免使用动作前的旧快照。
6. `product/prod_status_guard.js` 执行在线检测、异常识别及风控任务。
7. `report/report_rent_status.js` 组织用户维度的通知结果。

涉及商品状态、黑名单、跨渠道上下架或人脸识别问题时，应优先阅读：

- `product/PRODUCT_PRD.md`
- `product/PROD_GUARD_PRD.md`
- `blacklist/PRD_BLACKLIST.md`
- `order/PRD_ORDER.md`

### 3.3 订单与收益统计链路

- 订单写链路：`order/order_worker.js` -> `order/order.js` -> 各平台 API -> `order/service/order_command_service.js` -> 订单数据库。
- 订单读链路：业务模块 -> `order/service/order_query_service.js` -> 订单数据库。
- 统计刷新：`stats/order_stats_worker.js` -> `stats/order_stats.js` -> 订单查询服务 -> 日统计、成本和周快照表。
- H5 统计展示：`h5/local_h5_server.js` -> `stats/order_stats.js` 的 Dashboard、日历和账号明细查询。

涉及收益缺失、日期中断、统计口径时，应同时核查源订单、账号与游戏映射、统计任务状态、日统计结果和 H5 查询范围，不能只检查页面展示。设计背景见 `stats/PRD_STATS.md`。

### 3.4 H5 与服务分层

- H5/API 入口：`h5/local_h5_server.js`。
- BFF：`h5/h5_bff/`，只负责路由、鉴权、参数和响应编排。
- 领域逻辑：`order/`、`product/`、`stats/`、`blacklist/`、`price/` 等 Service。
- 数据访问：`database/*_db.js`，数据库连接统一从 `database/sqlite_client.js` 获取。
- H5 范围强制规则：`h5/AGENTS.md`，所有 H5 新页面和组件改动必须同时遵守。
- 前端组件和样式基线：`h5/COMPONENT_GUIDE.md`、`h5/public/css/app.css`。

### 3.5 数据、日志与运行记录

- 主业务库及拆分库：`database/*.db`，具体连接关系以 `database/sqlite_client.js` 为准。
- 数据库迁移：`database/migrations/`、`database/migration_runner.js`、`scripts/run_migrations.js`。
- 运行日志：`log/`。
- 异步任务和运行状态：`database/runtime_task_db.js` 及对应运行库。
- 新建表必须包含：`id`、`create_date`、`modify_date`、`is_deleted`、`desc`。

## 4. 四种默认工作模式

### 4.1 强制方案确认模式

任何可能修改代码、配置、数据库结构、脚本或生产环境的任务，都必须先完成方案确认，不以用户是否主动提出“给方案”为前提：

1. 收到修改、修复、优化或新增功能请求后，第一阶段默认只读，不修改代码、配置、数据库、脚本和生产环境。
2. 先把需求归入上述核心链路，阅读对应 PRD、入口文件及直接调用模块。
3. 定向追踪真实调用链、状态变化、数据表、配置和已有测试；不要求每次通读全仓库。
4. 先向用户复述当前机制、需求理解和问题边界，再讨论方案；存在关键歧义时明确列为待确认项，不得自行补全需求。
5. 方案至少包含：目标、现状证据、根因或限制、备选方案、推荐方案、准确改动点、数据兼容、风险、测试、发布与回滚。
6. 涉及高风险策略时至少提供两个可比较方案，并说明复杂度、影响范围和失败行为。
7. 与用户反复确认方案细节，直到用户针对当前方案明确表示“落地”“按此实现”或其他等价批准，才允许开始修改。
8. 用户在最初请求中直接说“修改”“修复”“实现”或“帮我做”，不视为已经完成方案 Review，仍必须先提交方案等待批准。
9. 批准只覆盖已经 Review 的改动范围；实施中发现需要扩大范围、改变核心行为、增加数据迁移或调整生产流程时，暂停修改并重新确认。

### 4.2 实现与测试模式

用户批准方案并要求落地后：

1. 先确认本次变更范围和未提交文件，保留用户已有改动，不做无关重构。
2. 优先复用现有 Service、数据库访问层、测试工具和前端组件。
3. 测试必须覆盖与改动相关的正常、边界、异常和回归路径；按风险补充幂等、并发、超时、重试、部分失败、日期边界和历史数据兼容测试。
4. 本次新增或实质修改的核心业务文件，行覆盖率必须达到 `90%` 以上。不能通过排除难测代码、空断言或只执行不验证结果来凑覆盖率。
5. 覆盖率范围必须明确列出；同时报告行、分支、函数覆盖率。若仓库全量覆盖率未达到 90%，不得把局部覆盖率描述成全仓覆盖率。
6. 优先运行 `package.json` 已有的定向测试和覆盖率命令；共享链路变更还需运行相关回归测试。
7. 测试不得直接修改生产数据或调用生产写接口。需要外部接口时优先使用 stub、fixture 或只读请求。
8. 生成供用户 Review 的测试报告，至少包含：测试范围、环境、用例分类、执行结果、覆盖率、失败项、未覆盖风险和报告路径。

### 4.3 生产发布与数据同步模式

仅当用户明确说“发布生产”或“发布宿主机”时，才授权执行生产发布：

1. 必须加载并遵循项目 Skill：`.agents/skills/production-release/SKILL.md`。
2. “落地”“提交代码”“合并代码”“测试通过”等表达不构成生产发布授权。
3. 项目唯一发布执行入口是 `scripts/merge_code.sh`，不得临时拼装另一套发布命令或绕过 Skill 的发布前检查。
4. 脚本包含覆盖同步和本机代码对齐步骤，执行前必须确认重要未提交代码已被保护；运行时不得中途改写脚本语义。
5. 发布结束必须核验并反馈：提交/分支、迁移结果、进程或服务状态、`/api/ping`、数据库同步、日志同步和异常项。
6. 任一步失败必须如实指出失败阶段和当前环境状态，不得在未验证时宣称发布成功，也不得擅自执行未定义的回滚或生产修复。
7. 发布脚本和文档不得新增明文密码、令牌或其他密钥；优先使用环境变量或密钥管理方式。

### 4.4 问题定位模式

用户要求“排查”“定位原因”“复现问题”时：

1. 默认只读排查，可以直接执行本地测试、只读数据库查询、日志检索和安全的 GET/查询请求。
2. 先判断本地数据库和日志是否足够新：时间范围必须覆盖问题发生时间，且相关同步任务在该时间段有记录。满足时优先本地排查。
3. 本地证据不足或过期时，先同步生产数据库和日志；如仍不足，再连接生产执行只读查询。未经明确授权不得执行生产写操作、补数据、重启或发布。
4. 按证据链排查：用户/游戏/账号映射 -> 调度与运行任务 -> 平台源数据/订单 -> 领域规则 -> 数据库存储/统计快照 -> H5/API 展示。
5. 复现必须说明输入、时间范围、环境和实际结果；不能使用会改变真实业务状态的方式复现上下架、黑名单或订单写操作。
6. 输出必须区分直接证据、推断和待验证项，并给出：现象、影响范围、根因、触发条件、为何只影响该时段或对象、修复选项、推荐方案和回归测试建议。
7. 完成根因分析后必须提交修复方案并等待用户明确批准；即使用户最初同时要求“排查并修复”，也不得跳过方案 Review 直接修改代码或数据。

## 5. 通用改动与交付规则

- 修改前检查 `git status`；数据库和日志的运行态变化不得被误当作代码改动提交。
- 不覆盖、不回滚用户未明确授权处理的改动。
- 不使用 `git reset --hard`、`git clean` 等破坏性命令，除非用户明确触发的既有发布脚本包含该步骤，并且已完成发布前保护检查。
- H5、BFF、通知、数据库和组件规范继续遵循 `config/codex_rule.md`。
- 代码改动完成后，反馈实际改动文件、行为变化、测试结果、覆盖率、剩余风险；不能只给结论。
- 如只完成了分析、测试或排查，应明确说明没有修改代码、数据库或生产环境。
