# 悠悠租号改价接口测试报告

## 实现范围

- 新增 `modifyProductPrice(productId, input, options)`。
- 复用现有 `appKey + timestamp + sign` 鉴权流程。
- 根据时租价自动计算套餐价，统一保留两位小数：
  - 2 小时：`hourPrice * 2 * 0.9`
  - 3 小时：`hourPrice * 3 * 0.8`
  - 5/7/9/10 小时：`hourPrice * hours * 0.7`
  - 24/168 小时：`hourPrice * hours * 0.6`
- 悠悠服务端当前不把 `p2Price` 纳入验签字段，因此该字段保留在请求体中，但仅在本接口签名时排除。

## 自动化测试

- 命令：`node test/uuzuhao_modify_price_api_smoke_test.js`
- 结果：通过。
- 覆盖：价格计算、两位小数、参数校验、签名兼容、请求字段、成功响应、平台错误，以及现有悠悠公开接口回归。
- `uuzuhao/uuzuhao_api.js`：行覆盖率 91.81%，分支覆盖率 73.89%，函数覆盖率 96.77%。
- HTML 报告：`coverage/uuzuhao-modify-price/index.html`。

## 真实接口联调

- 用户：uid=8。
- 账号：`2943214278`。
- 商品：`202602191001484920`。
- 调用前状态：号主下架，时租 2 元，起租 2 小时。
- 提交价格：时租 2、2小时 3.6、3小时 4.8、5小时 7、7小时 9.8、9小时 12.6、10小时 14、24小时 28.8、168小时 201.6。
- 平台响应：`code=0`，`msg=成功`。
- 调用后回读：商品仍为下架，时租仍为 2 元，起租仍为 2 小时。

## 已知限制

悠悠商品列表接口当前只返回时租价和起租时长，不返回各套餐价格，因此无法通过该接口逐项回读套餐价格；本次以提交载荷和改价接口成功响应确认套餐写入。
